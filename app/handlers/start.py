"""
FileConverter Bot - Обработчики команд /start и /help
"""

from aiogram import Router
from aiogram.filters import CommandStart, Command
from aiogram.types import Message

# Создаём роутер для обработки команд
router = Router(name="start")


# Текст приветственного сообщения
START_MESSAGE = """
🔄 <b>Добро пожаловать в FileConverter Bot!</b>

Я помогу вам конвертировать файлы в различные форматы.
Просто отправьте мне файл, и я предложу доступные варианты конвертации.

<b>📁 Поддерживаемые форматы:</b>

🖼️ <b>Изображения:</b>
   • JPEG, PNG, WEBP, BMP, HEIC → JPEG, PNG, WEBP, BMP, PDF

📄 <b>Документы:</b>
   • DOCX, PDF, TXT, RTF, ODT → PDF, TXT, DOCX

📊 <b>Таблицы:</b>
   • XLSX, CSV → CSV, XLSX

🎵 <b>Аудио:</b>
   • MP3, OGG, WAV, FLAC → MP3, OGG, WAV, FLAC

🎬 <b>Видео:</b>
   • MP4, AVI, MOV, MKV → MP4, AVI, MKV

<b>⚠️ Ограничения:</b>
• Максимальный размер файла: <b>50 МБ</b>

<i>Отправьте файл, чтобы начать конвертацию!</i>
"""

# Текст справки
HELP_MESSAGE = """
📖 <b>Справка по использованию бота</b>

<b>Как конвертировать файл:</b>
1️⃣ Отправьте файл боту (как документ или медиафайл)
2️⃣ Выберите желаемый формат из предложенных кнопок
3️⃣ Дождитесь завершения конвертации
4️⃣ Получите готовый файл!

<b>📌 Доступные команды:</b>
• /start — начать работу с ботом
• /help — показать эту справку

<b>💡 Советы:</b>
• Для лучшего качества используйте исходные файлы высокого разрешения
• Видеофайлы конвертируются дольше из-за перекодирования
• При конвертации аудио сохраняется максимальное качество

<b>❓ Возникли проблемы?</b>
Убедитесь, что файл не повреждён и соответствует ограничениям размера.

<i>Отправьте файл, чтобы начать!</i>
"""


@router.message(CommandStart())
async def cmd_start(message: Message) -> None:
    """
    Обработчик команды /start
    Отправляет приветственное сообщение с информацией о боте
    """
    await message.answer(
        START_MESSAGE,
        parse_mode="HTML"
    )


@router.message(Command("help"))
async def cmd_help(message: Message) -> None:
    """
    Обработчик команды /help
    Отправляет справочное сообщение
    """
    await message.answer(
        HELP_MESSAGE,
        parse_mode="HTML"
    )
